<div id="feeds">
  <h3>Feeds!</h3>
  <ul>
  <% for entry in FeedEntry.all(:limit => 10, :order => "published_date desc") %>
    <li><%= link_to h(entry.name), entry.url %></li>
  <% end %>
  </ul>
</div>